import LoginForm from "@/components/forms/loginForm";

export default function LoginPage() {
  return (
    <div
      className={
        "font-inter flex flex-col items-center justify-center h-screen"
      }
    >
      <LoginForm />
    </div>
  );
}
